= title t('tt.index', resource_name: ProjectPicture.model_name.human)

- content_for :toolbar_left do
  = link_to(admins_project_path(@project), class: 'btn', rel: 'tooltip', title: t('tooltips.show')) do
    %i.icon-arrow-left


%ul
  = render @project_pictures

.clearfix
%hr
= render 'form'
= render :partial => "admins/modal"